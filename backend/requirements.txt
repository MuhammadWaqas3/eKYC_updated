# fastapi==0.109.0
# uvicorn==0.27.0
# python-multipart==0.0.17
# pydantic==2.12.5
# pydantic-settings==2.12.0
# httpx==0.26.0
# python-dotenv==1.0.0
# PyJWT==2.11.0
# cryptography==44.0.0
# python-jose==3.5.0
# easyocr==1.7.2
# numpy==1.26.4
# Pillow==11.3.0
# opencv-python-headless==4.10.0.84
# torch==2.2.0
# torchvision==0.17.0
# ultralytics==8.4.14
# mediapipe==0.10.9
# groq==1.0.0
# supabase==2.28.0



# ─────────────────────────────────────────────────────────────────────────────
# eKYC Backend — requirements.txt
# dlib aur face-recognition HATAO — yahi build fail ka reason tha
# face matching ab insightface (pure ONNX, no compile) se hoga
# ─────────────────────────────────────────────────────────────────────────────

# Server
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.17
pydantic==2.12.1
pydantic-settings==2.2.1
httpx==0.26.0
python-dotenv==1.0.0

# Auth
PyJWT==2.8.0
cryptography==42.0.5
python-jose==3.3.0

# CV / AI
numpy==1.26.4
Pillow==10.3.0
opencv-python-headless==4.9.0.80

# Torch — CPU only (smaller, faster download than default)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# OCR
easyocr==1.7.2

# YOLO
ultralytics==8.1.47

# Face liveness (mediapipe — pure Python wheel, no compile)
mediapipe==0.10.14

# Face MATCHING — replaces dlib/face_recognition
# insightface uses ONNX runtime — NO compilation needed
insightface==0.7.3
onnxruntime==1.17.3

# Chatbot
groq==0.5.0

# Database
supabase==2.4.3